package semmiedev.disc_jockey;

import net.fabricmc.api.DedicatedServerModInitializer;

public class ServerInitializer implements DedicatedServerModInitializer {
    @Override
    public void onInitializeServer() {
        // This code runs only on the dedicated server environment.
        // It's used for registering server-side things like commands and packet handlers.
    }
} 